#include<stdio.h>
int main()
{
  int a,b,c,d;
  printf("enter the value of a,b,c,d:");
  scanf("%d%d%d%",&a,&b,&c,&d);
   if(a>b && a>c && a>d ){

   if (b>c && b>d)
   {

    if(c>d) }
    }

   {printf("ascending order is %d%d%d%d",); }

   return 0;


}

